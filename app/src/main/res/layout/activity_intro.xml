<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorAccent">

    <ImageView
        android:src="@drawable/logo_ver"
        android:layout_width="150dp"
        android:layout_height="200dp"
        android:layout_centerInParent="true"/>

    <LinearLayout
        android:orientation="vertical"
        android:id="@+id/denied_Layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center_horizontal"
        android:visibility="gone">

        <Button
            android:layout_width="300dp"
            android:layout_height="60dp"
            android:text="@string/permission_re"
            android:textSize="20dp"
            android:textColor="@color/intro_btn_txt"
            android:textAllCaps="false"
            android:background="@color/intro_btn_back"
            android:onClick="PermissionClicked"/>

        <Button
            android:layout_width="300dp"
            android:layout_height="60dp"
            android:layout_marginTop="30dp"
            android:text="@string/app_exit"
            android:textSize="20dp"
            android:textColor="@color/intro_btn_txt"
            android:textAllCaps="false"
            android:background="@color/intro_btn_back"
            android:onClick="ExitClicked"/>

    </LinearLayout>


</RelativeLayout>